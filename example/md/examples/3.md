```vue
<timeago 
  :datetime="datetime2" 
  :converter="isFutureOrPastConverter" 
/>
```

<br>

```js
// Component Methods
methods: {
  isFutureOrPastConverter (date, locale, options) {
    return new Date(date) > Date.now()
      ? 'Date is Future'
      : 'Date is Past'
  }
}
```
